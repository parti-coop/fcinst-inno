/ Main container
.page-container
  / bloc-16
  #bloc-16.bloc.lake-hero-bloc.bgc-onyx.d-bloc{:class => "bg-gradation-background"}
    = render 'application/header'
    .container
      .row
        .col-sm-12.interview-header
          %h3.mg-md.tc-safety-orange-blaze-orange
            인터뷰
          %p
            사회 혁신의 맨 앞이기에
            %br.visible-xs
            누구보다 먼저 겪는 어려움.
            %br>/
            어떻게 해결할 수 있을까요?
            %br/
    .container.fill-bloc-bottom-edge
      = render 'pages/social_share_panel', from: 'interviews_list', icon_color: 'white'
  / bloc-16 END
  / bloc-17
  - if @interviews.any?
    #bloc-17.bloc.tc-white.bgc-onyx-2
      .container.bloc-lg.interview-list-container
        / - abort @interviews.inspect
        - @interviews.each_slice(2) do |interviews_slices|
          .row
            - interviews_slices.each do |interview|
              = link_to interview_path(interview) do
                .col-sm-6.col-xs-12
                  = render 'interview_card', interview: interview



  / bloc-17 END
/ Main container END
